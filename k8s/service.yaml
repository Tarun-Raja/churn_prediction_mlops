apiVersion: v1
kind: Service
metadata:
  name: churn-api-service
spec:
  # A LoadBalancer distributes incoming customer traffic evenly across our 2 replicas
  type: LoadBalancer
  selector:
    app: churn-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000